import { beforeAll, afterAll } from "vitest";
import { db } from "../db/index.js";

beforeAll(async () => {
  // Configurações de teste ou reset de DB (se necessário)
});

afterAll(async () => {
  // Limpeza de DB (se necessário)
});
